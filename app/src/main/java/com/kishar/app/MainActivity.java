package com.kishar.app;import android.app.*;import android.os.*;import android.content.*;import android.graphics.*;import android.view.*;import android.widget.*;import android.security.keystore.*;import android.util.Base64;import java.util.*;import java.security.*;import javax.crypto.*;import javax.crypto.spec.*;import okhttp3.*;import org.json.*;public class MainActivity extends Activity{ final int OLIVE=Color.rgb(105,132,67),INK=Color.rgb(25,28,24),BG=Color.rgb(248,249,246),RED=Color.rgb(173,55,45); LinearLayout body;OkHttpClient http;WebSocket socket;Timer heartbeatTimer;boolean connected=false,simulation=true;long seq=0;String server="",robotId="",token=""; int d(int n){return(int)(n*getResources().getDisplayMetrics().density+.5f);} TextView tx(String s,int z,int c){TextView v=new TextView(this);v.setText(s);v.setTextSize(z);v.setTextColor(c);v.setPadding(d(14),d(10),d(14),d(10));return v;} Button bt(String s){Button b=new Button(this);b.setText(s);b.setTextSize(16);b.setTextColor(INK);b.setAllCaps(false);b.setMinHeight(d(52));return b;} void add(View v){body.addView(v,new LinearLayout.LayoutParams(-1,-2));} void title(String s){TextView v=tx(s,25,INK);v.setTypeface(null,1);add(v);} void card(String s){TextView v=tx(s,16,INK);v.setBackgroundColor(Color.WHITE);LinearLayout.LayoutParams p=new LinearLayout.LayoutParams(-1,-2);p.setMargins(0,d(5),0,d(5));body.addView(v,p);} void frame(){body=new LinearLayout(this);body.setOrientation(LinearLayout.VERTICAL);body.setPadding(d(16),d(8),d(16),d(16));body.setBackgroundColor(BG);ScrollView s=new ScrollView(this);s.addView(body);setContentView(s);} void clear(String s){body.removeAllViews();title(s);} void back(){Button b=bt("رجوع للرئيسية");b.setOnClickListener(v->home());add(b);} void stop(){sendTool("stop_robot",new JSONObject());} void emergency(){Button b=bt("إيقاف طوارئ");b.setTextColor(Color.WHITE);b.setBackgroundColor(RED);b.setOnClickListener(v->{stop();Toast.makeText(this,connected?"تم إرسال STOP إلى الخادم":"لا يوجد اتصال — تم إيقاف المحاكي",1).show();});add(b);} public void onCreate(Bundle b){super.onCreate(b);load();home();} String keyAlias="kishar_config"; ja
